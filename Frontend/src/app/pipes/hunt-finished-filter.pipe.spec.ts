import { HuntFinishedFilterPipe } from './hunt-finished-filter.pipe';

describe('HuntFinishedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HuntFinishedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
