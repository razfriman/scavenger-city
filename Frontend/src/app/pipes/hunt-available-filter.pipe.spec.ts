import { HuntAvailableFilterPipe } from './hunt-available-filter.pipe';

describe('HuntAvailableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HuntAvailableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
